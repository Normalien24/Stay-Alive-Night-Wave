{
  "name": "stay-alive-night-wave",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.166.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.3.0"
  }
}
